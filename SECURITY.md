# 安全策略

本项目高度重视安全性。感谢你帮助我们一起保护 **JSON 格式化与查看工具** 及其用户。

## 支持的版本

本项目持续在 `main` 分支上维护，安全修复会发布到最新版本。请始终使用最新版以获得安全更新。

## 项目安全特性

本项目是一个**纯前端的单文件应用**，安全模型相对简单：

- 所有 JSON 解析与渲染**完全在浏览器本地进行**，不上传、不存储任何用户数据；
- 不依赖任何后端服务、数据库或第三方分析脚本；
- 「从 URL 导入」功能受浏览器**同源策略（CORS）**约束，工具仅能读取明确允许跨域访问的公开接口数据。

## 报告漏洞

如果你发现安全漏洞，**请不要在公开的 Issue 中提交**，以免被恶意利用。请通过以下任一私密渠道报告：

1. **GitHub 私密安全建议（推荐）**  
   前往 https://github.com/wangruofeng/json-viewer/security/advisories/new 提交。  
   （若仓库未启用，请在仓库的 Security 标签页选择「Report a vulnerability」。）

2. **GitHub Issues 标注为安全相关**  
   如无法使用上述渠道，可在 [Issues](https://github.com/wangruofeng/json-viewer/issues) 中提交，但请勿包含可被直接利用的细节或 PoC，先与维护者取得联系。

报告时请尽量包含：

- 漏洞的清晰描述与影响范围；
- 复现步骤或最小可复现样例；
- 你建议的修复思路（可选）。

## 响应流程

- 收到报告后，维护者会在合理时间内确认收悉；
- 我们将与报告者协作核实问题，并在修复方案确定后协调披露；
- 对于确认有效的安全漏洞，我们会在致谢名单（如报告者同意）中予以感谢。

请不要针对生产环境进行未授权的破坏性测试。
